# Idea Flow Notes: mathematics_7_may2020_uKRa3LH15IKUdk5n — towfish in response to various parameters, however this is impossible without advanced computing

## Paragraph Flow (move by move)

1. **P1**: "software, because the x component..." → **mechanism** (identifies loop dependency). Hands to next via **cause** ("but Rx depends on...").
2. "The function f(x) depends on Tx..." → **chain of dependency**. Hands via **consequence** ("thus an insolvable loop").
3. "This loop is however eliminated..." → **solution/introduction of workaround**. Hands via **condition** ("making Rx negligible").
4. "The vector Tx can be used..." → **limitation acknowledged**. Hands via **reason** ("since vector calculus is out of scope").
5. "As mentioned in the first axiom..." → **reference to prior principle**. Hands via **logical bridge** ("Thus, the only condition...").
6. "For any values of x..." → **generalization of condition**. Hands via **observation** ("By considering...figure 6").
7. "This is because p is the only vector..." → **explanation of uniqueness**. Hands via **specification** ("Since the only parameter...").
8. "this important property is displayed..." → **visual confirmation**. Hands via **transition** ("Since the only parameter...").

## What This Section Does (content sequence)

1. **Identifies a problem** (loop dependency) – sets up need for resolution.
2. **Proposes a workaround** (thin wire assumption) – narrows scope to solvable case.
3. **States governing principle** (force equilibrium) – provides rule for modeling.
4. **Derives condition for f(x)** – applies rule to specific context.
5. **Analyzes vector components** – isolates changing variable.
6. **Visualizes result** – confirms logic via figure.

Each step sets up the next by narrowing assumptions or applying principles.

## Paragraph Skeletons (replicable templates)

**SKELETON 1:**  
"[Problem caused by X]. This intrigued me as [why it mattered], since [general principle]. [Term] is defined as [definition]; in [context] it [application]."  
- Slot 1: Describe a conflict or contradiction.  
- Slot 2: Explain personal or theoretical relevance.  
- Slot 3: Define key term.  
- Slot 4: Apply term to context.  
- Original: "software, because the x component... loop dependency."  
- Demo: "The algorithm failed due to recursion depth. This mattered because stack limits constrain execution. Recursion is defined as self-reference; in Python it causes stack overflow."

**SKELETON 2:**  
"[Workaround introduced]. [Limitation acknowledged]. [Principle cited]. [Condition derived]."  
- Slot 1: State simplification.  
- Slot 2: Note constraint.  
- Slot 3: Reference axiom/law.  
- Slot 4: Derive usable condition.  
- Original: "This loop is however eliminated... vector sum of all forces... is 0."  
- Demo: "The equation is linearized. Calculus tools are unavailable. Newton’s first law applies. Equilibrium requires net force zero."

## Express-Idea Vocabulary

- **Sequencing**: "thus an insolvable loop occurs"  
- **Cause/Consequence**: "since vector calculus is out of scope"  
- **Contrast/Concession**: "This loop is however eliminated"  
- **Specification**: "the only component that changes"  
- **Evidence handling**: "As mentioned in the first axiom"  
- **Explanation verbs**: "is dependent on", "can be explained by"

## How to Explain an Idea (replication steps)

**Pattern**: Problem → Workaround → Principle → Derivation → Verification

1. State the core issue blocking progress.
2. Introduce simplifying assumption to bypass issue.
3. Cite governing principle or law.
4. Apply principle under assumption to derive usable form.
5. Confirm derivation using visual or numerical example.
